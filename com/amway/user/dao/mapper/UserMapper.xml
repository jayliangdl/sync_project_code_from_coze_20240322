<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.amway.user.dao.UserDao">

    <insert id="createUser" parameterType="com.amway.user.model.UserDO">
        INSERT INTO uc_user (user_name, gender, status, psw)
        VALUES (#{userName}, #{gender}, #{status}, #{password})
    </insert>

    <update id="updateUser" parameterType="com.amway.user.model.UserDO">
        UPDATE uc_user
        SET user_name = #{userName},
            psw = #{password},
            gender = #{gender}
        WHERE ID = #{id}
    </update>

    <update id="deleteUser" parameterType="java.lang.Integer">
        UPDATE uc_user
        SET status = '2'
        WHERE ID = #{userId}
    </update>

    <select id="queryUser" resultType="com.amway.user.model.UserDO">
        SELECT * FROM uc_user
        WHERE ID = #{userId} OR user_name = #{userName}
        LIMIT 1
    </select>

</mapper>